const $=id=>document.getElementById(id);function show(id,btn){document.querySelectorAll('.panel').forEach(x=>x.classList.remove('active'));$(id).classList.add('active');document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));btn.classList.add('active');$('title').textContent={chat:'درخشا',tree:'درخت دانش',engine:'موتور درخشا',settings:'تنظیمات'}[id];if(id==='tree')loadTree()}function newChat(){$('messages').innerHTML='';show('chat',document.querySelector('nav button'))}function example(t){$('input').value=t;$('input').focus()}function key(e){if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}}function add(type,text){const d=document.createElement('div');d.className='msg '+type;d.innerHTML=text;$('messages').appendChild(d);d.scrollIntoView({behavior:'smooth'})}async function send(){const text=$('input').value.trim();if(!text)return;add('user',text);$('input').value='';add('ai','در حال تحلیل جمله، استخراج کلیدواژه و جستجو در درخت دانش…');try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text,style:$('style').value})});const d=await r.json();document.querySelector('#messages .ai:last-child').remove();add('ai',`${d.answer}<div class="meta">اعتماد: ${d.confidence}% · مسیر: ${d.knowledge_path.join(' ← ')||'یافت نشد'} · کلیدواژه‌ها: ${d.keywords.join('، ')}</div>`)}catch(e){document.querySelector('#messages .ai:last-child').textContent='ارتباط با موتور برقرار نشد.'}}async function loadTree(){const r=await fetch('/api/tree');const d=await r.json();$('treeBox').innerHTML=node(d)}function node(n){return `<div class="node"><b>${n.name}</b>${(n.children||[]).map(node).join('')}</div>`}
